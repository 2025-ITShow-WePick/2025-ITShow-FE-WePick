import React, { useEffect } from 'react';

const PostLocationInput = () => {

  return (
    <div>
      <label>위치(지점)</label>
      <input type="text" placeholder="클릭해서 사진을 찍은 위치와 지점을 입력해 주세요" />
    </div>
  );
};

export default PostLocationInput;
