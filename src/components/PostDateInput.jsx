import React, { useEffect } from 'react';

const PostDateInput = () => {

  return (
    <div>
      <label>날짜</label>
      <input type="date" placeholder='YYYY / MM / DD' />
    </div>
  );
};

export default PostDateInput;
